h3 运行环境
	button.btn.btn-success.btn-xs.pull-right(ng-click="add()") 创建运行环境

table.table.table-hover
	tbody: tr(ng-repeat="env in environment" ng-click="editEnvironment(env)" style="cursor: pointer;" ng-class="{ danger: env.removed }")
		td {{ env.name }}