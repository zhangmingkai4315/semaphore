h3 密钥
	button.btn.btn-success.btn-xs.pull-right(ng-click="add()") 创建密钥

table.table.table-hover
	tbody: tr(ng-repeat="key in keys" ng-click="update(key)" style="cursor: pointer;" ng-class="{ danger: key.removed }")
		td
			code {{ key.type }}
			| &nbsp; {{ key.name }}