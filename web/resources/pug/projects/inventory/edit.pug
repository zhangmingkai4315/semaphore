.modal-header
	h3.modal-title 编辑主机清单

.modal-body(style="padding: 0")
	div(ui-ace="{mode: 'ini', workerPath: '/public/js/ace/'}" style="height: 200px" ng-model="inventory")
.modal-footer
	button.btn.btn-default.pull-left(ng-click="$dismiss()") 取消
	button.btn.btn-success(ng-click="$close(inventory)") 保存变更